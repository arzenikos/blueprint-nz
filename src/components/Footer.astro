---
// This file is kept for compatibility but the actual Footer component has moved to src/components/common/Footer.astro
// Importing the new Footer component to maintain backward compatibility
import Footer from './common/Footer.astro';

// Re-export the Footer component
export { Footer as default };
---
