---
// This file is kept for compatibility but icon functionality has been moved to NavIcons.astro
// and other specialized icon components
import NavIcons from './NavIcons.astro';

export interface Props {
  icon: string;
}

const { icon } = Astro.props as Props;
---

<NavIcons name={icon} />
