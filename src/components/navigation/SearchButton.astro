<!-- ---
import Icon from '../icons/Icon.astro';

interface Props {
  iconSize?: number;
  class?: string;
}

const { iconSize = 20, class: className = '' } = Astro.props;
---

<button class={`md:hidden p-2 rounded-full hover:bg-neutral-100 dark:hover:bg-gray-700 ${className}`} aria-label="Search">
  <Icon name="search" size={iconSize} class="text-secondary dark:text-gray-300" />
</button> -->
