<!-- ---
// src/components/MapCanvas.tsx
import React, { useEffect, useRef, useState } from 'react';
import GridTile from './GridTile';
import DescriptionPanel from './DescriptionPanel';
import type { NodeInfo, Position } from './types';

const gridSize = 6; // Define a 6x6 grid

// Static node data
const nodes: NodeInfo[] = [
  { x: 2, y: 2, description: 'You found the Forest Node 🌲' },
  { x: 4, y: 1, description: 'This is the Mountain Node ⛰️' },
];
  const [pos, setPos] = useState<Position>({ x: 0, y: 0 });
  const [activeNode, setActiveNode] = useState<NodeInfo | null>(null);
  const containerRef = useRef<HTMLDivElement>(null);

---
<div ref={containerRef} className="relative w-full h-full">
  <div class="grid grid-cols-6 gap-1 w-fit p-2 mx-auto">
    {Array.from({ length: gridSize * gridSize }, (_, i) => {
      const x = i % gridSize;
      const y = Math.floor(i / gridSize);
      return <GridTile key={i} position={{ x, y }} playerPos={pos} nodes={nodes} />;
    })}
  </div>

  <DescriptionPanel node={activeNode} />
</div>

<script>
  export default  MapCanvas: React.FC = () => {
  
    // Handle key events for movement
    useEffect(() => {
      const handleKeyDown = (e: KeyboardEvent) => {
        setPos((prev) => {
          const newPos = { ...prev };
          if (e.key === 'ArrowUp') newPos.y = Math.max(0, newPos.y - 1);
          if (e.key === 'ArrowDown') newPos.y = Math.min(gridSize - 1, newPos.y + 1);
          if (e.key === 'ArrowLeft') newPos.x = Math.max(0, newPos.x - 1);
          if (e.key === 'ArrowRight') newPos.x = Math.min(gridSize - 1, newPos.x + 1);
          return newPos;
        });
      };
  
      window.addEventListener('keydown', handleKeyDown);
      return () => window.removeEventListener('keydown', handleKeyDown);
    }, []);
  
    // Check if player is on a node
    useEffect(() => {
      const found = nodes.find((node) => node.x === pos.x && node.y === pos.y);
      setActiveNode(found || null);
    }, [pos]);
      //
  };
</script> -->

<!-- export default MapCanvas; -->

