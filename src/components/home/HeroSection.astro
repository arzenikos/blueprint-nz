---
import { Image } from 'astro:assets';
import { homeData } from '../../data/home';
import Container from '../ui/Container.astro';
import Button from '../ui/Button.astro';
import Markdown from '../common/Markdown.astro';
import PixelRibbon from '../ui/PixelRibbon.astro';
import placeholderImage from '../../assets/images/placeholder-image.jpg';

const { hero } = homeData;
---

<section class="py-12">
  <Container>
    <div class="flex flex-col md:flex-row items-center gap-8 mb-10 w-full">
      <PixelRibbon pixelSize={2} className="mb-2" />
      <div class="w-full md:w-1/2">
        <div class="mb-6">
          <h1 class="mb-2">{hero.title}</h1>
        </div>
        <Markdown content={hero.slogan} class="mb-8 text-lg" />
        <Button href={hero.ctaLink} size="lg">{hero.ctaText}</Button>
      </div>
      <div class="w-full md:w-1/2">
        <Image
          src={placeholderImage}
          alt="Hero image"
          class="w-full max-w-full mx-auto rounded-lg shadow-lg"
          width={800}
          height={600}
          loading="eager"
          format="webp"
          quality={80}
        />
      </div>
    </div>
  </Container>
</section>
