---
// <!-- Imported by: layouts/BaseLayout.astro -->
import { footerLinks } from '../../../content/footer';
import FooterSection from '../footer/FooterSection.astro';
import NewsletterForm from '../footer/NewsletterForm.astro';
import SocialLinks from '../footer/SocialLinks.astro';

// Get spacing values from centralized file
---

<footer class="w-full border-t border-secondary/20 bg-secondary mt-95 dark:bg-gray-900" style={`
`}>
  <div class="container mx-auto max-w-[2000px]">
    <nav aria-label="Secondary" class="flex flex-col lg:flex-row justify-between gap-10 lg:gap-16 pt-6">
      <!-- Newsletter Section -->
      <NewsletterForm 
        title={footerLinks.newsletterSection.title}
        description={footerLinks.newsletterSection.description}
        placeholder={footerLinks.newsletterSection.placeholder}
        buttonText={footerLinks.newsletterSection.buttonText}
      />
      
      <!-- Footer Sections -->
      <div class="grid grid-cols-2 sm:grid-cols-4 gap-8 sm:gap-10">
        {footerLinks.sections.map((section, index) => (
          <FooterSection 
            title={section.title} 
            links={section.links} 
            index={index} 
          />
        ))}
      </div>
    </nav>
    
    <hr class="border-primary/30 my-6" style="border-color: var(--color-palette-four);">
    
    <div class="flex flex-col md:flex-row items-center gap-4">
      <div class="text-xs text-center md:text-left" style="color: var(--color-palette-gray);">
        {footerLinks.copyright}
      </div>
      <div>
        <SocialLinks links={footerLinks.socialLinks.map(link => ({
          label: link.platform,
          href: link.url,
          icon: link.platform.toLowerCase()
        }))} />
      </div>
    </div>
  </div>
</footer>

<style>
  footer .container{
    margin-top: 2rem;
    /* padding-top: 2rem; */
    padding-bottom: 2rem;
    padding-left: 1rem;
    padding-right: 1rem;
  }

  @media (min-width: 640px) {
    footer .container {
      padding-left: 1.5rem;
      padding-right: 1.5rem;
    }
  }
  @media (min-width: 1024px) {
    footer .container {
      padding-left: 2rem;
      padding-right: 2rem;
    }
  }

  /* footer hr {
    padding-top: 2.5rem;
    padding-bottom: 0.5rem;
  } */
</style>