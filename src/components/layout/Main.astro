---
// <!-- Imported by: layouts/BaseLayout.astro, layouts/FullPageLayout.astro -->
// import Logo from './Logo.astro';
// import FontSizeToggle from './FontSizeToggle.astro';
// import ThemeToggle from './ThemeToggle.astro';
// import { mainNavItems } from '../../../content/navigation';

const {
  class: 
  // mobileOnly = false,
  className = '' ,
//   desktopOnly = true,
} = Astro.props;

// const visibilityClass = desktopOnly ? 
// 'justify-between' 
// : 
// '';
---

<header class="sticky top-0 my-5 z-40 w-full bg-transparent">
  <div class=`container mx-auto px-10 md:px-6 lg:px-8 max-w-[2000px] flex items-center h-20`>
    <h1>Main</h1>
    <!-- Logo (left) - Size from centralized file -->
     <!-- <Logo/> -->
    <!-- Right Navigation Items -->
    <div class={`flex items-center py-2 gap-4`}>
        <!-- {mainNavItems.map(item => ( -->
        <div class={`flex items-center space-x-1 sm:space-x-8 ${className}`}>
          <!-- <ThemeToggle /> -->
        </div>
      </div>
  </div>
</header>
