---
// <!-- Imported by: components/home/ServicesSection.astro, components/layout/Footer.astro -->
// import PixelRibbon from './PixelRibbon.astro';

// interface Props {
//   title?: string;
//   subtitle?: string;
//   id?: string;
//   class?: string;
//   showRibbon?: boolean;
// }

// const { 
//   title, 
//   subtitle, 
//   id, 
//   class: className = '',
//   showRibbon = true
// } = Astro.props;
---
<!-- 
<section id={id} class={`py-8 md:py-12 ${className}`}>
  {(title || subtitle) && (
    <div class="text-center mb-8 md:mb-10">
      <div class="mb-4 md:mb-6">
        {title && <h2 class="text-2xl md:text-3xl font-bold mb-3 md:mb-2">{title}</h2>}
        {showRibbon && <PixelRibbon pixelSize={2} class="mx-auto w-24" />}
      </div>
      {subtitle && <p class="text-lg text-gray-600">{subtitle}</p>}
    </div>
  )}
  <slot />
</section> -->
