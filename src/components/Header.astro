---
// This file is kept for compatibility but the actual Header component has moved to src/components/common/Header.astro
// Importing the new Header component to maintain backward compatibility
import Header from './common/Header.astro';

// Re-export the Header component
export { Header as default };
---
