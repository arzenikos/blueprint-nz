<!-- ---
// src/components/CardAccordion.astro
import items from '../react/data.json';

// group by category, or whatever grouping you need for the accordion
const groups = Object.groupBy(items, (item) => item.category);
---

<div class="accordion">
  {Object.entries(groups).map(([category, cards]) => (
    <details class="accordion-item">
      <summary>{category}</summary>
      <div class="card-grid">
        {cards.map((card) => (
          <a class="card" href={`/products/${card.slug}`}>
            <img src={card.image} alt={card.title} />
            <h3>{card.title}</h3>
            <p>{card.description}</p>
          </a>
        ))}
      </div>
    </details>
  ))}
</div>

<style>
  .card-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
    gap: 1rem;
    padding: 1rem 0;
  }
  .card {
    display: block;
    text-decoration: none;
    color: inherit;
    border: 1px solid #ddd;
    border-radius: 8px;
    padding: 1rem;
    transition: transform 0.15s ease;
  }
  .card:hover {
    transform: translateY(-2px);
  }
  .accordion-item summary {
    cursor: pointer;
    font-weight: 600;
    padding: 0.75rem 0;
  }
</style> -->