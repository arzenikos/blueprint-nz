---
interface Props {
  columns?: 2 | 3 | 4;
}
const { columns = 3 } = Astro.props;

const colClasses = {
  2: 'sm:grid-cols-2',
  3: 'sm:grid-cols-2 md:grid-cols-3',
  4: 'sm:grid-cols-2 md:grid-cols-4',
};
---
<div class:list={['grid gap-4 grid-cols-1', colClasses[columns]]}>
  <slot />
</div>