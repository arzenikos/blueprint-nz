<!-- Not imported by any file -->
---
// ChatBox.astro - Virtual agent chat box component
---

<!-- <div id="chat-box-container" class="fixed bottom-6 right-6 z-50"> -->
  <!-- Chat Icon Button -->
  <!-- <button 
    id="chat-toggle-btn"
    class="chat-toggle-btn bg-[var(--color-secondary)] hover:bg-[var(--color-secondary-light)] text-[var(--color-secondary-content)] rounded-full p-3 shadow-lg flex items-center justify-center w-14 h-14 transition-all duration-300"
    aria-label="Open chat with Prometheus"
  >
    <svg xmlns="http://www.w3.org/2000/svg" class="h-7 w-7" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 10h.01M12 10h.01M16 10h.01M9 16H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-5l-5 5v-5z" />
    </svg>
  </button> -->

  <!-- Chat Box -->
  <!-- <div id="chat-box" class="hidden bg-white rounded-lg shadow-xl w-80 sm:w-96 overflow-hidden transition-all duration-300 transform translate-y-0 opacity-100 flex-col max-h-[500px] border border-[var(--color-border)]"> -->
    <!-- Chat Header -->
    <!-- <div class="bg-[var(--color-secondary)] text-[var(--color-secondary-content)] p-4 flex justify-between items-center">
      <div class="flex items-center gap-2">
        <div class="w-8 h-8 rounded-full bg-[var(--color-primary)] flex items-center justify-center">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 10h.01M12 10h.01M16 10h.01M9 16H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-5l-5 5v-5z" />
          </svg>
        </div>
        <h3 class="font-medium">Prometheus</h3>
      </div>
      <div class="flex items-center gap-2">
        <button id="minimize-chat" class="text-[var(--color-secondary-content)] hover:text-[var(--color-primary-light)]" aria-label="Minimize chat">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18 12H6" />
          </svg>
        </button>
        <button id="close-chat" class="text-[var(--color-secondary-content)] hover:text-[var(--color-primary-light)]" aria-label="Close chat">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>
    </div> -->
    
    <!-- Chat Messages Area -->
    <!-- <div class="flex-1 p-4 overflow-y-auto bg-[var(--color-muted)] min-h-[320px] chat-messages">
      <div class="space-y-4">
        <div class="flex items-start">
          <div class="w-8 h-8 rounded-full bg-[var(--color-primary)] flex items-center justify-center mr-2 flex-shrink-0">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 10h.01M12 10h.01M16 10h.01M9 16H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-5l-5 5v-5z" />
            </svg>
          </div>
          <div class="bg-white rounded-lg p-3 max-w-[80%] shadow-sm border border-[var(--color-border)]">
            <p class="text-sm">Hello! I'm Prometheus, your virtual assistant. How can I help you today?</p>
          </div>
        </div>
      </div>
    </div> -->
    
    <!-- Chat Input -->
    <!-- <div class="border-t border-[var(--color-border)] p-3 bg-white">
      <div class="flex items-center">
        <input 
          type="text" 
          placeholder="Type your message..." 
          class="flex-1 border border-[var(--color-border)] rounded-l-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-[var(--color-primary)]"
          disabled
        />
        <button 
          class="bg-[var(--color-secondary)] text-[var(--color-secondary-content)] px-4 py-2 rounded-r-lg hover:bg-[var(--color-secondary-light)] disabled:opacity-50 flex items-center justify-center"
          disabled
        >
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8" />
          </svg>
        </button>
      </div>
      <div class="mt-2 text-xs text-center text-gray-500">
        Prometheus AI Assistant - Demo Version
      </div>
    </div> -->
  <!-- </div> -->
<!-- </div> -->

<!-- <script>
  // Client-side JavaScript for chat box functionality
  document.addEventListener('DOMContentLoaded', () => {
    const chatToggleBtn = document.getElementById('chat-toggle-btn');
    const chatBox = document.getElementById('chat-box');
    const closeChat = document.getElementById('close-chat');
    const minimizeChat = document.getElementById('minimize-chat');
    
    // Toggle chat box visibility
    chatToggleBtn?.addEventListener('click', () => {
      chatBox?.classList.toggle('hidden');
      if (!chatBox?.classList.contains('hidden')) {
        chatToggleBtn.classList.add('hidden');
        
        // Add entrance animation class
        chatBox?.classList.add('chat-box-entrance');
        
        // Store chat state in localStorage
        localStorage.setItem('chatBoxOpen', 'true');
      }
    });
    
    // Close chat box
    closeChat?.addEventListener('click', () => {
      chatBox?.classList.add('hidden');
      chatToggleBtn?.classList.remove('hidden');
      
      // Store chat state in localStorage
      localStorage.setItem('chatBoxOpen', 'false');
    });
    
    // Minimize chat box (same as close for now)
    minimizeChat?.addEventListener('click', () => {
      chatBox?.classList.add('hidden');
      chatToggleBtn?.classList.remove('hidden');
      
      // Store chat state in localStorage
      localStorage.setItem('chatBoxOpen', 'false');
    });
    
    // Check if chat was previously open
    const chatWasOpen = localStorage.getItem('chatBoxOpen') === 'true';
    if (chatWasOpen) {
      chatBox?.classList.remove('hidden');
      chatToggleBtn?.classList.add('hidden');
    }
  });
</script> -->
