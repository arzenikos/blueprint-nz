<!-- Referenced by: pages/blobs/index.astro, pages/newsletter.astro -->
<!-- ---
import { marked } from 'marked';

interface Props {
  content: string;
  class?: string;
}

const { content, class: className = '' } = Astro.props;

// Parse markdown to HTML
const html = marked.parse(content);
---

<div class={`markdown ${className}`} set:html={html} />

<style is:global>
  .markdown > * {
    margin-top: 1.5rem;
    margin-bottom: 1.5rem;
  }

  .markdown h1, 
  .markdown h2, 
  .markdown h3, 
  .markdown h4, 
  .markdown h5, 
  .markdown h6 {
    font-weight: bold;
  }

  .markdown h1 {
    font-size: 2.25rem;
  }

  .markdown h2 {
    font-size: 1.875rem;
  }

  .markdown h3 {
    font-size: 1.5rem;
  }

  .markdown h4 {
    font-size: 1.25rem;
  }

  .markdown p {
    line-height: 1.625;
  }

  .markdown a {
    color: var(--color-primary);
    text-decoration: underline;
  }

  .markdown a:hover {
    opacity: 0.8;
  }

  .markdown ul, 
  .markdown ol {
    padding-left: 1.5rem;
  }

  .markdown ul {
    list-style-type: disc;
  }

  .markdown ol {
    list-style-type: decimal;
  }

  .markdown blockquote {
    padding-left: 1rem;
    border-left-width: 4px;
    border-color: #d1d5db;
    font-style: italic;
  }

  .markdown pre {
    padding: 1rem;
    border-radius: 0.375rem;
    background-color: #1f2937;
    color: white;
    overflow-x: auto;
  }

  .markdown code {
    font-family: monospace;
    font-size: 0.875rem;
  }

  .markdown :not(pre) > code {
    padding: 0.125rem 0.25rem;
    border-radius: 0.25rem;
    background-color: #f3f4f6;
    color: #111827;
  }

  .markdown img {
    max-width: 100%;
    border-radius: 0.375rem;
  }

  .markdown hr {
    margin-top: 2rem;
    margin-bottom: 2rem;
    border-color: #e5e7eb;
  }

  .markdown table {
    width: 100%;
    border-collapse: collapse;
  }

  .markdown th, 
  .markdown td {
    border-width: 1px;
    padding: 0.5rem;
    text-align: left;
  }

  .markdown th {
    background-color: #f9fafb;
  }
</style> -->
