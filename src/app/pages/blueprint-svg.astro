---
// Redirect to the static SVG file instead
export const prerender = true;

// Set up a client-side redirect
const redirectUrl = '/blueprint.svg';
---

<meta http-equiv="refresh" content={`0;url=${redirectUrl}`}>
<script>
  window.location.href = '{redirectUrl}';
</script>
