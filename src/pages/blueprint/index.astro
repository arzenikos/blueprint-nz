---
import Layout from '../../layouts/Layout.astro';
import Breadcrumbs from '../../components/ui/Breadcrumbs.astro';
import Card from '../../components/ui/Card.astro';
import { blueCardConfig } from '../../lib/types/CardLibrary'
import { blueCards } from '../../content/blueindex';

const breadcrumbItems = [
  { label: 'BluePrint', href: '/bluprint', current: true }
];

const {
  icon = blueCardConfig.icon,
  textClass = blueCardConfig.textClass,
  color = blueCardConfig.color,
  extraConfig = blueCardConfig.extraConfig,
} = blueCardConfig;

const blueCardsContent = [
  "BlueBeacon",
  "BlueNode",
  "TrueBlueStats",
];
---
<Layout title="BluePrint - BluePrintNZ">
  <Breadcrumbs items={breadcrumbItems} />
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 w-full px-4 md:px-10 py-10 place-items-center">
   {blueCards.map(({heading, content },index) => (
    <Card
      heading={heading}
      content={content}
      icon={icon}
      textClass={textClass}
      color={color}
      config={extraConfig}
      index={index}
      />
    ))}
  </div>
</Layout>